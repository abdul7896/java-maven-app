pipeline {
    agent any  // Runs on any available agent

    stages {
        // Your stages will go here
        stage('Example') {
            steps {
                // Your steps will go here
                echo 'Hello, Jenkins!'
            }
        }
    }
}
